1. Pixel Shuffle requires Python 3.4 and PIL(Python Imaging Library)

2. When saving, you must manually specify the file extension.

3. Wherever you click on the canvas, keep in mind that is where the top left corner(coordinate x1) of the
bounding rectangle will be drawn.

4. Images with dimensions that exceed those of the monitor will currently be rejected.
 
